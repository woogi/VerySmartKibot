<RelativeLayout xmlns:android="http://schemas.android.com/apk/res/android"
    android:id="@+id/main_layout"
    android:layout_width="fill_parent"
    android:layout_height="fill_parent"
    android:orientation="vertical" >

    <FrameLayout
        android:id="@+id/layout_normal"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent" >

        <ImageView
            android:id="@+id/face_image"
            android:layout_width="fill_parent"
            android:layout_height="fill_parent"
            android:orientation="horizontal"
            android:src="@drawable/face_mode1_e" >
        </ImageView>
    </FrameLayout>
    

   

    <LinearLayout
        android:id="@+id/layout"
        android:layout_width="fill_parent"
        android:layout_height="fill_parent"
        android:orientation="vertical" >
    </LinearLayout>

    <TextView
        android:id="@+id/robot_mode"
        android:layout_width="100dp"
        android:layout_height="20dp"
        android:gravity="center"
        android:textColor="#ff000000"
        android:layout_alignParentRight="true"
        android:layout_marginRight="140dp"
        android:layout_marginTop="10dp"
        android:background="#ffffff00"
    />
    
    <ScrollView
        android:layout_width="wrap_content"
        android:layout_height="wrap_content"
        android:layout_marginLeft="10dp" >

        <TextView
            android:id="@+id/log_view"
            android:layout_width="wrap_content"
            android:layout_height="wrap_content"
            android:paddingBottom="10dp"
            android:textColor="#000" />
        
    </ScrollView>

     <ImageView
        android:contentDescription="ear image"
        android:id="@+id/listen_image"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:orientation="horizontal"
        android:layout_alignParentLeft="true"
       
        android:src="@drawable/listen"
        android:visibility="visible">
    </ImageView>
    
    <ImageView
        android:contentDescription="login image"
        android:id="@+id/logIn"
        android:layout_width="100dp"
        android:layout_height="100dp"
        android:orientation="horizontal"
        android:layout_toRightOf="@id/listen_image"
        android:layout_alignBaseline="@id/listen_image"
        android:src="@drawable/out"
        android:visibility="visible">
    </ImageView>
    
</RelativeLayout>
